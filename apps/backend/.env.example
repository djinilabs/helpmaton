# Environment Variables for Helpmaton Backend
# See ENV.md for detailed documentation

# ============================================================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================================================

# Secret key used for JWT token signing and encryption
# Generate with: openssl rand -base64 32
# or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=

# Mailgun API key for sending authentication emails
# Get from: https://www.mailgun.com → Domain settings → API key
MAILGUN_KEY=

# Mailgun domain for sending emails (defaults to helpmaton.com if not set)
MAILGUN_DOMAIN=helpmaton.com

# Base URL of your application (used for generating magic link URLs)
# Development: http://localhost:3333
# Production: https://app.helpmaton.com
BASE_URL=http://localhost:3333

# Google Gemini API key for AI agent functionality
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# ============================================================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================================================

# Path to local sandbox database files (persists data across restarts)
# Only used in local development (sandbox mode)
ARC_DB_PATH=./db

# Sentry DSN for error tracking and monitoring
# Get from: https://sentry.io → Project settings → DSN
# SENTRY_DSN=

# ============================================================================
# EMAIL OAUTH CONFIGURATION
# ============================================================================

# Google OAuth 2.0 client ID for Gmail email connections
# Get from: https://console.cloud.google.com → Credentials → OAuth client ID
# GOOGLE_OAUTH_CLIENT_ID=

# Google OAuth 2.0 client secret for Gmail email connections
# Created together with GOOGLE_OAUTH_CLIENT_ID
# GOOGLE_OAUTH_CLIENT_SECRET=

# Microsoft Azure AD application (client) ID for Outlook email connections
# Get from: https://portal.azure.com → Azure AD → App registrations
# OUTLOOK_CLIENT_ID=

# Microsoft Azure AD client secret for Outlook email connections
# Get from: Azure AD app registration → Certificates & secrets
# OUTLOOK_CLIENT_SECRET=

# Base URL for OAuth callback redirects
# Development with frontend proxy: http://localhost:5173
# Development with direct backend: http://localhost:3333
# Production: https://app.helpmaton.com
# Must match redirect URIs configured in Google Cloud Console and Azure Portal exactly
# Do not include trailing slash
# OAUTH_REDIRECT_BASE_URL=http://localhost:5173

# Frontend application URL for OAuth callback redirects
# Defaults to http://localhost:5173 in development if not set
# FRONTEND_URL=http://localhost:5173

# ============================================================================
# S3 CONFIGURATION
# ============================================================================

# S3 bucket name for storing workspace documents
# Defaults to workspace.documents if not set
HELPMATON_S3_BUCKET=workspace.documents

# S3 endpoint URL (for local development with s3rver)
# Only used in local development (defaults to http://localhost:4568)
# In production, uses standard AWS S3 endpoint
HELPMATON_S3_ENDPOINT=http://localhost:4568

# AWS access key ID for S3 access
# Required in production when not using IAM roles
# Falls back to AWS_ACCESS_KEY_ID if not set
# HELPMATON_S3_ACCESS_KEY_ID=

# AWS secret access key for S3 access
# Required in production when not using IAM roles
# Falls back to AWS_SECRET_ACCESS_KEY if not set
# HELPMATON_S3_SECRET_ACCESS_KEY=

# AWS region where the S3 bucket is located
# Defaults to AWS_REGION or eu-west-2 if not set
# HELPMATON_S3_REGION=eu-west-2
