# Only install optional dependencies for linux-arm64
# This prevents downloading LanceDB binaries for other platforms (darwin, windows, etc.)
supportedArchitectures[os]=linux
supportedArchitectures[cpu]=arm64
